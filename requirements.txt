# Core Dependencies
numpy>=1.20.0
opencv-python>=4.5.0
matplotlib>=3.3.0
Pillow>=8.0.0
requests>=2.25.0
djitellopy>=2.4.0

# GUI Framework (for HUD)
# tkinter is included with Python, but may need additional packages on some systems

# AI/ML Dependencies
transformers>=4.20.0
torch>=1.9.0
torchvision>=0.10.0

# Optional GPU support (uncomment if needed)
# torch-audio>=0.9.0  # For advanced audio processing

# Development and Utilities
typing-extensions>=4.0.0

# Note: OLLAMA dependencies are in ollama_tools/requirements_ollama.txt 